class Solution(object):
    def findDuplicate(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        hashMap = {}
        for num in nums:
            if num not in hashMap:
                hashMap[num]=1
            else:
                return num
        
        
        